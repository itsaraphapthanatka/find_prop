/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

// ค่าที่ inject ตอน build (vite.config.ts → define) — commit ของโค้ดชุดนี้ ใช้โดย live-update
declare const __BUILD_ID__: string
